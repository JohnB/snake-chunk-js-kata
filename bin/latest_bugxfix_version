#!/bin/sh

export current_minor_branch=`git symbolic-ref head | cut -c 12-15`
export last_bugfix_branch=`git branch | grep $current_minor_branch |  sort | head -1 | cut -c 3-`
git checkout $last_bugfix_branch

